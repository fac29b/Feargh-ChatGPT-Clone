// conversationStorage.js
const conversationHistory = [];

modules.exports = {
    getConversation: () => [...converation],
    addConversation: (conversation) => {
        conversationHistory.push(conversation);
    },
};